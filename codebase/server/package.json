{
  "name": "enigma-d2-server",
  "version": "0.1.0",
  "private": true,
  "description": "Proxy server cho demo D2 'Học từ lỗi' — gọi Claude (Anthropic API), OpenRouter hoặc Ollama",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "smoke": "node scripts/smoke-test.js",
    "hints": "node scripts/generate-hints.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.90.0"
  }
}
